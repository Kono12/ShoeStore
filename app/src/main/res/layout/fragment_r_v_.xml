<?xml version="1.0" encoding="utf-8"?>

<!--i used constraint layout instead of linear layout-->
<!--to separate the button from the Text views in the-->
<!--linear lay out-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RV_Fragment"
    android:background="@color/white">

    <Button
        android:id="@+id/hbd"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:text="+"
        android:layout_marginTop="20dp"
        android:background="@color/Orange"
        android:textSize="32sp"
        />


    <ScrollView
        app:layout_constraintTop_toBottomOf="@id/hbd"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        >

        <!--    linear layout rapped with scrollView to be able to scroll -->
        <!--    and gave the linear layout an id to be able to add views later-->
        <LinearLayout
            android:layout_marginTop="20dp"
    android:id="@+id/LinearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
            android:gravity="center"

    >




</LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>